import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return (
    <PagePlaceholder
      title="Performance — Ad Run Status"
      legacyRef="getAdStatusData + submitPartnershipId"
      description="All posted ad-eligible posts. Classification badges (Winner / ITE / Discarded / Discarded but analyse). Partnership ID input."
    />
  );
}
