import { PagePlaceholder } from "@/components/ui/page-placeholder";
export default function Page() {
  return (
    <PagePlaceholder
      title="Journey"
      legacyRef="getInfluencerList + updateInfluencerStatus"
      description="Kanban: Reach Out → On Board → Posted → Delivered → RTO / Cancelled."
    />
  );
}
