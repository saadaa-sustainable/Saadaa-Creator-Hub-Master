# Public — exposed to the browser
NEXT_PUBLIC_SUPABASE_URL=https://xynyvbagcudjrzklwnqp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never prefix NEXT_PUBLIC_
SUPABASE_SERVICE_KEY=
SHOPIFY_WEBHOOK_SECRET=

# Email — routed through the legacy GAS MailApp endpoint (NOT Resend / SES).
# Deploy a GAS web app exposing doPost that wraps MailApp.sendEmail and verifies HMAC.
GAS_EMAIL_ENDPOINT=https://script.google.com/macros/s/AKfycb.../exec
GAS_EMAIL_SECRET=
GAS_EMAIL_FROM_NAME=Saadaa Creator Hub

# Instagram scraping — Apify only, on a 3-hour cron.
# lookupCreator queues fresh handles into instagram_cache(status='pending');
# the `scrape-pending-apify` Supabase Edge Function picks them up every 3 hours.
APIFY_TOKEN=
APIFY_ACTOR_ID=apify/instagram-profile-scraper

GOOGLE_SHEETS_SERVICE_ACCOUNT_JSON=
SPREADSHEET_ID=1HdB_D_xorlbCxOHHtrW_jyyfq6YiEAe_81Mal7Td3HQ
